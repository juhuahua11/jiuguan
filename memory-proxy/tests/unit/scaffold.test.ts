// Placeholder test to ensure vitest finds at least one test file
import { describe, it, expect } from 'vitest';

describe('Project scaffold', () => {
  it('should have a working test runner', () => {
    expect(true).toBe(true);
  });
});
